//  a function named sum that takes two parameters
//and returns their sum.
function sum(num1, num2) {
  return num1 + num2;
}
console.log(sum(75, 4));

// function called logMessage that accepts a string
//as a parameter and logs it to the console.
function logMessage(str) {
  console.log(str);
}
logMessage("Hello You!");
